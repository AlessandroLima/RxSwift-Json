# Uncomment the next line to define a global platform for your project
 platform :ios, '12.0'

target 'TesteJson' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift', '~> 4.5'
  pod 'RxCocoa', '~> 4.5'
  pod 'Kingfisher', '~> 5.0'
  

  # Pods for TesteJson

  target 'TesteJsonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TesteJsonUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
